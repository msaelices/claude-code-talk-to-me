# TalkToMe Local Mode Configuration
# Copy this file to .env.local and adjust settings as needed

# ====================================
# AUDIO SYSTEM CONFIGURATION
# ====================================

# Audio system to use (pulseaudio, pipewire, alsa)
TALKTOME_AUDIO_SYSTEM=pulseaudio

# ====================================
# TEXT-TO-SPEECH (TTS) CONFIGURATION
# ====================================

# TTS provider (piper for local, openai for cloud)
TALKTOME_TTS_PROVIDER=piper

# Path to Piper ONNX model file
# Download models with: python3 download-models.py
TALKTOME_PIPER_MODEL_PATH=models/piper/en_US-amy-medium.onnx

# Speaking speed (length_scale): lower = faster, higher = slower
# Range: 0.5 (very fast) to 2.0 (very slow), default: 1.0 (normal)
# Examples: 0.7 = fast, 0.85 = slightly fast, 1.0 = normal, 1.2 = slow, 1.5 = very slow
TALKTOME_PIPER_SPEED=1.0

# ====================================
# SPEECH-TO-TEXT (STT) CONFIGURATION
# ====================================

# STT provider (whisper for local, openai for cloud)
TALKTOME_STT_PROVIDER=whisper

# Whisper model to use (tiny, base, small, medium, large-v3)
# Smaller = faster but less accurate, Larger = slower but more accurate
TALKTOME_WHISPER_MODEL=base

# Device for Whisper inference (auto, cpu, cuda)
TALKTOME_WHISPER_DEVICE=auto

# Compute type for Whisper (auto, int8, float16)
# int8 = faster on CPU, float16 = faster on GPU
TALKTOME_WHISPER_COMPUTE_TYPE=auto

# Silence duration in milliseconds before considering speech complete
TALKTOME_STT_SILENCE_DURATION_MS=800

# ====================================
# OPTIONAL: OPENAI CONFIGURATION
# ====================================

# If you want to use OpenAI for either TTS or STT:
# TALKTOME_OPENAI_API_KEY=sk-xxx
# TALKTOME_TTS_VOICE=onyx  # For OpenAI TTS
# TALKTOME_STT_MODEL=gpt-4o-transcribe  # For OpenAI STT

# ====================================
# SERVER SETTINGS
# ====================================

# Port for HTTP server (used for health checks)
TALKTOME_PORT=3000

# ====================================
# LOGGING
# ====================================

# Log level (debug, info, warn, error)
# LOG_LEVEL=info